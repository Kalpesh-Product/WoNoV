services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: redis-prod
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    environment:
      - REDIS_ARGS=--requirepass yourpassword
    restart: always

volumes:
  redis-data:
