{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "sh -c 'if [ \"$VERCEL_BRANCH\" = \"main\" ]; then exit 1; else exit 0; fi'",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
