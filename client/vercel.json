{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "if [ \"$VERCEL_GIT_BRANCH\" = \"main\" ]; then exit 1; else exit 0; fi",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
